services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: myapp-prod
    ports:
      - "3000:3000"
    volumes:
      - .:/app          
      - /app/node_modules
    environment:
      - NODE_ENV=production 

