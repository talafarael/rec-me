services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: myapp-prod
    ports:
      - "3000:3000"
    env_file:
      - .env.production
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped 

